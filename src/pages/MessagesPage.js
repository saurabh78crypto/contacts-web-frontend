import React from 'react';
import SentMessages from '../components/SentMessage';

const MessagesPage = () => {
  return (
    <div>
      <SentMessages />
    </div>
  );
};

export default MessagesPage;
